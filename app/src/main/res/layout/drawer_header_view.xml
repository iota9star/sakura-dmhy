<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/image_view_banner"
        android:layout_width="match_parent"
        android:layout_height="@dimen/v256dp"
        android:scaleType="centerCrop"
        tools:ignore="ContentDescription" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginLeft="@dimen/v16dp"
        android:layout_marginStart="@dimen/v16dp"
        android:maxLines="1"
        android:text="@string/app_name"
        android:textColor="#96000000"
        android:textSize="@dimen/v64sp"
        android:textStyle="bold|italic" />

    <de.hdodenhof.circleimageview.CircleImageView
        android:layout_width="@dimen/v92dp"
        android:layout_height="@dimen/v92dp"
        android:layout_gravity="center"
        android:src="@mipmap/app_icon"
        app:civ_border_color="@color/md_white_1000"
        app:civ_border_width="@dimen/v4dp" />
</FrameLayout>
